cockpit Logindaten liegen unter /doc/cockpit_login.txt


## Docker

Netzwerk anlegen:

`docker network create ha-net-internal`


Einmalig den Login auf registry.webpro.fuman.de hinterlegen:

`docker login registry.webpro.fuman.de -ulaurenz`

